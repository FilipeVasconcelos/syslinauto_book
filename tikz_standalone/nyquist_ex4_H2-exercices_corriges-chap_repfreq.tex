\documentclass[tikz,border=3pt]{standalone}
\input{preamble/tikz}
\input{preamble/fmv}
\begin{document}
\begin{tikzpicture}
\begin{axis}[
            axis x line=center,
            axis y line=center,
            xlabel style={below right},
            ylabel style={left},
            x tick label style={above},
            y tick label style={right},
            width=0.6\textwidth,
            xlabel={$\Re{H_2(\jw)}$},
            ylabel={$\Im{H_2(\jw)}$},
            xmin=-1.1, xmax=1.1,
            ymin=-1.1, ymax=1.1,
            clip =false
        ]
        \coordinate (insetPosition) at (axis cs: 0.75,0.75);
        \addplot[domain=0:0.5,signalb,-{Latex[length=3mm]}]
        ({((1-x*x)*(1-x*x)-6*x*x)/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))},
         {(-5*x*(1-x*x))/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))});
        \addplot[domain=0.4:2,signalb]
        ({((1-x*x)*(1-x*x)-6*x*x)/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))},
         {(-5*x*(1-x*x))/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))});
        \addplot[domain=1.9:10,signalb]
        ({((1-x*x)*(1-x*x)-6*x*x)/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))},
         {(-5*x*(1-x*x))/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))});
        \addplot[domain=9.9:30,signalb]
        ({((1-x*x)*(1-x*x)-6*x*x)/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))},
         {(-5*x*(1-x*x))/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))});
         \draw[draw=none,fill=col1] 
         (axis cs:-0.333333,0) circle[radius=1.5pt] node[above] {I};
    \end{axis}
    \begin{axis}[at={(insetPosition)},
        grid=none,
        anchor={center},
        footnotesize,
        ticklabel style = {font=\scriptsize,
        /pgf/number format/fixed,
        /pgf/number format/precision=5},
        scaled ticks=false,
        axis x line=center,
        axis y line=center,
        xlabel style={below right},
        ylabel style={above left},
        x tick label style={above},
        y tick label style={right},
        width=0.3\textwidth,
        xtick={-0.02},
        ytick={0.04},
        xticklabels={-0.02},
        yticklabels={0.04},
        xmin=-0.03, xmax=0.03,
        ymin=-0.03, ymax=0.15,
        ]
        \addplot[domain=1:30,signalb]
                ({((1-x*x)*(1-x*x)-6*x*x)/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))},
                 {(-5*x*(1-x*x))/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))});
        \addplot[domain=1:3,signalb,-{Latex[length=2mm]}]
                ({((1-x*x)*(1-x*x)-6*x*x)/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))},
                 {(-5*x*(1-x*x))/((1+x*x)*((1-x*x)*(1-x*x)+9*x*x))});
    \end{axis}
\end{tikzpicture}
\end{document}
